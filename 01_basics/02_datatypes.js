"use strict"; // Treat all Js code as newer version

// alert(3 + 3); // we are using nodejs, not browser

let name = "Javascript";
let age = 20;
let isLoggedIn = false;
let state = null;

// nmuber => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone Value
// undefined => 
// symbol => unqiue

// object 

console.log(typeof undefined); // typeof is undefined
console.log(typeof null); // typeof is object
